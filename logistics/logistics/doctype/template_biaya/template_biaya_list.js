/*
frappe.listview_settings['Template Biaya'] = {
  add_fields: ["jenis"],
	get_indicator: function(doc) {
		if(doc.jenis==="Import") {
			return [__("Import"), "blue", "jenis,=,Import"];
		} else if(doc.jenis==="Export") {
			return [__("Export"), "green", "jenis,=,Export"];
		} else if(doc.jenis==="Trucking") {
			return [__("Trucking"), "red", "jenis,=,Trucking"];
		}
	}
};
*/
